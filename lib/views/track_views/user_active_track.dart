import 'package:flutter/material.dart';

class UserActiveTrack extends StatelessWidget {
  const UserActiveTrack({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
